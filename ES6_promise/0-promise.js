export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // You can perform asynchronous operations here.
    // For this basic task, returning a new Promise is enough to pass the check.
  });
}
